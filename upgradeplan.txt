Backend:
	* Make register route not need admin
	* Manage users (delete, add both permissions)
	Timesheet post
	Timesheet delete

Frontend:
	Register page, (link to in login)
	Timesheet post, (make the new auth json variable and stuff)
	Timesheet delete,
	Manage user page, (make auth routes bruh)